# System Flow Diagrams

Visual representations of how the Inbox Context Graph works.

## Flow 1: First Decision (No Precedent)

```
┌─────────────┐
│    USER     │
│ (Frontend)  │
└──────┬──────┘
       │ 1. Click message
       ▼
┌─────────────────────────────────────────┐
│         MessageList Component            │
│  [Investor Message from Sarah Chen]     │
└──────┬──────────────────────────────────┘
       │ 2. onMessageSelect(message)
       ▼
┌─────────────────────────────────────────┐
│       MessageDetail Component            │
│  • Shows message content                 │
│  • Calls API for suggestion              │
└──────┬──────────────────────────────────┘
       │ 3. POST /agent/suggest/{msg_id}
       ▼
┌─────────────────────────────────────────┐
│         FastAPI Backend                  │
│  AgentEngine.get_suggestion()            │
│  ┌────────────────────────────────┐     │
│  │ 1. Query for similar decisions │     │
│  │    → 0 found (first time)      │     │
│  │ 2. Apply base logic            │     │
│  │    → investor = reply_now +    │     │
│  │      neutral tone              │     │
│  │ 3. Return suggestion           │     │
│  └────────────────────────────────┘     │
└──────┬──────────────────────────────────┘
       │ 4. Returns: {action: "reply_now", 
       │              tone: "neutral",
       │              reasoning: "No precedent found",
       │              precedent_count: 0}
       ▼
┌─────────────────────────────────────────┐
│       MessageDetail Component            │
│  ┌───────────────────────────────────┐  │
│  │ 🤖 Agent Suggests:                │  │
│  │ reply_now • neutral               │  │
│  │ "No precedent found. Using        │  │
│  │  default logic for investor."     │  │
│  └───────────────────────────────────┘  │
│                                          │
│  USER OVERRIDES:                         │
│  [reply_now] [warm] ← User choice        │
│                                          │
│  [Confirm Decision] ← Click              │
└──────┬──────────────────────────────────┘
       │ 5. POST /decisions
       │    {message_id, 
       │     agent_suggestion: {reply_now, neutral},
       │     human_action: {reply_now, warm},
       │     context_used: {sender_type: investor},
       │     why: "..."}
       ▼
┌─────────────────────────────────────────┐
│         FastAPI Backend                  │
│  1. Create Decision record               │
│  2. Update Graph:                        │
│     • Message node                       │
│     • Decision node                      │
│     • Action node (reply_now)            │
│     • Tone node (warm)                   │
│     • SenderType node (investor)         │
│     • Edges connecting all               │
│  3. Commit to PostgreSQL                 │
└──────┬──────────────────────────────────┘
       │ 6. Returns decision_id
       ▼
┌─────────────────────────────────────────┐
│    Frontend - Shows success              │
│    "Decision captured!"                  │
│    • Decision count increments           │
│    • Graph updated                       │
└─────────────────────────────────────────┘
```

---

## Flow 2: Fifth Decision (With Precedent)

```
┌─────────────┐
│    USER     │
│ (Frontend)  │
└──────┬──────┘
       │ 1. Click another investor message
       ▼
┌─────────────────────────────────────────┐
│         MessageDetail Component          │
└──────┬──────────────────────────────────┘
       │ 2. POST /agent/suggest/{msg_id}
       ▼
┌─────────────────────────────────────────┐
│         FastAPI Backend                  │
│  AgentEngine.get_suggestion()            │
│  ┌────────────────────────────────────┐ │
│  │ 1. HYBRID RETRIEVAL                │ │
│  │    ┌─────────────────────────────┐ │ │
│  │    │ a) Get message embedding    │ │ │
│  │    │    via OpenAI API           │ │ │
│  │    │ b) Filter by sender_type:   │ │ │
│  │    │    investor                 │ │ │
│  │    │ c) Calculate cosine         │ │ │
│  │    │    similarity               │ │ │
│  │    │ d) Return top 5 similar     │ │ │
│  │    │    decisions                │ │ │
│  │    └─────────────────────────────┘ │ │
│  │    → Found 4 decisions!            │ │
│  │                                    │ │
│  │ 2. PRECEDENT ANALYSIS              │ │
│  │    • Decision 1: reply_now + warm  │ │
│  │    • Decision 2: reply_now + warm  │ │
│  │    • Decision 3: reply_now + warm  │ │
│  │    • Decision 4: reply_now + warm  │ │
│  │    → Pattern: 4/4 chose "warm"     │ │
│  │                                    │ │
│  │ 3. APPLY PRECEDENT                 │ │
│  │    Most common: reply_now + warm   │ │
│  │    Reasoning: "Based on 4 prior    │ │
│  │    investor messages, you usually  │ │
│  │    chose 'warm' tone."             │ │
│  └────────────────────────────────────┘ │
└──────┬──────────────────────────────────┘
       │ 3. Returns: {action: "reply_now",
       │              tone: "warm",  ← LEARNED!
       │              reasoning: "Based on 4...",
       │              precedent_count: 4}
       ▼
┌─────────────────────────────────────────┐
│       MessageDetail Component            │
│  ┌───────────────────────────────────┐  │
│  │ 🤖 Agent Suggests:                │  │
│  │ reply_now • warm                  │  │
│  │ "Based on 4 prior investor        │  │
│  │  messages, you usually chose      │  │
│  │  'warm' tone."                    │  │
│  │ ✨ Based on 4 similar decisions   │  │
│  └───────────────────────────────────┘  │
│                                          │
│  USER ACCEPTS (agent learned!)           │
│  [reply_now] [warm] ← Matches suggestion │
│                                          │
│  ✅ Accepting AI suggestion              │
│  [Confirm Decision] ← Click              │
└──────┬──────────────────────────────────┘
       │ 4. POST /decisions
       │    (agent_suggestion == human_action!)
       ▼
┌─────────────────────────────────────────┐
│         FastAPI Backend                  │
│  1. Create Decision record               │
│  2. Link to 4 precedent decisions        │
│     (DecisionPrecedent edges)            │
│  3. Update Graph with new nodes/edges    │
│  4. New decision becomes future          │
│     precedent for next message           │
└─────────────────────────────────────────┘

🎉 THE AGENT HAS LEARNED!
```

---

## Flow 3: Graph Visualization

```
┌─────────────┐
│    USER     │
└──────┬──────┘
       │ 1. Click "Context Graph" tab
       ▼
┌─────────────────────────────────────────┐
│         GraphViewer Component            │
└──────┬──────────────────────────────────┘
       │ 2. GET /graph
       ▼
┌─────────────────────────────────────────┐
│         FastAPI Backend                  │
│  1. Query graph_nodes table              │
│     SELECT * FROM graph_nodes            │
│  2. Query graph_edges table              │
│     SELECT * FROM graph_edges            │
│  3. Return {nodes: [...], edges: [...]}  │
└──────┬──────────────────────────────────┘
       │ 3. Returns graph data
       ▼
┌─────────────────────────────────────────┐
│         GraphViewer Component            │
│  1. Transform to React Flow format       │
│  2. Calculate node positions             │
│  3. Color-code by type:                  │
│     • Message → purple                   │
│     • Decision → blue                    │
│     • Action → yellow                    │
│     • Tone → orange                      │
│     • SenderType → green                 │
│  4. Render with React Flow               │
└──────┬──────────────────────────────────┘
       │ 4. Display interactive graph
       ▼
┌─────────────────────────────────────────┐
│         Visual Graph Display             │
│                                          │
│    [Message1] ─→ [Decision1]            │
│                      │                   │
│                      ├─→ [reply_now]     │
│                      ├─→ [warm]          │
│                      └─→ [investor]      │
│                                          │
│    [Message2] ─→ [Decision2]            │
│                      │                   │
│                      ├─→ [reply_now]     │
│                      ├─→ [warm]          │
│                      ├─→ [investor]      │
│                      │                   │
│                      └─→ [Decision1] ← precedent!
│                          (red edge)      │
└─────────────────────────────────────────┘
       │ 5. User clicks [Decision2]
       ▼
┌─────────────────────────────────────────┐
│         Decision Details Panel           │
│  Agent Suggested: reply_now • neutral    │
│  Human Chose: reply_now • warm           │
│  Reasoning: "Based on 1 prior..."        │
│  Timestamp: 2024-01-15 10:30:00         │
└─────────────────────────────────────────┘
```

---

## Data Flow: Hybrid Retrieval Deep Dive

```
┌──────────────────────────────────────────┐
│         New Message Arrives               │
│  "Hey, wanted to check in on metrics..." │
│  sender_type: investor                    │
└──────┬───────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│    1. EMBEDDING GENERATION                │
│    OpenAI text-embedding-3-small          │
│    "Hey wanted to check..." → [1536-dim]  │
│    embedding = [0.023, -0.145, 0.876...] │
└──────┬───────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│    2. STRUCTURED FILTER                   │
│    SQL: SELECT decisions.*                │
│         FROM decisions                    │
│         JOIN messages ON ...              │
│         WHERE messages.sender_type        │
│              = 'investor'                 │
│                                           │
│    Result: [dec1, dec2, dec3, dec4]       │
└──────┬───────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│    3. SEMANTIC SIMILARITY                 │
│    For each candidate decision:           │
│    • Get its message embedding            │
│    • Calculate cosine similarity          │
│                                           │
│    dec1: similarity = 0.87  ← Similar!    │
│    dec2: similarity = 0.82  ← Similar!    │
│    dec3: similarity = 0.45  ← Less        │
│    dec4: similarity = 0.91  ← Very similar!│
└──────┬───────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│    4. RANKING & SELECTION                 │
│    Sort by similarity, take top 5:        │
│    1. dec4 (0.91)                         │
│    2. dec1 (0.87)                         │
│    3. dec2 (0.82)                         │
│                                           │
│    Return: [dec4, dec1, dec2]             │
└──────┬───────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│    5. PATTERN EXTRACTION                  │
│    Analyze human_action from precedents:  │
│                                           │
│    dec4.human_action = {reply_now, warm}  │
│    dec1.human_action = {reply_now, warm}  │
│    dec2.human_action = {reply_now, neutral}│
│                                           │
│    Action counts:                         │
│      reply_now: 3                         │
│                                           │
│    Tone counts:                           │
│      warm: 2                              │
│      neutral: 1                           │
└──────┬───────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│    6. APPLY MAJORITY VOTE                 │
│    Most common action: reply_now (3/3)    │
│    Most common tone: warm (2/3)           │
│                                           │
│    Suggestion: reply_now + warm           │
│    Reasoning: "Based on 3 prior investor  │
│                messages, you usually      │
│                chose 'warm' tone."        │
└──────┬───────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│    7. RETURN TO FRONTEND                  │
│    {                                      │
│      "action": "reply_now",               │
│      "tone": "warm",                      │
│      "reasoning": "Based on 3 prior...",  │
│      "precedent_count": 3,                │
│      "similar_decisions": [               │
│        "dec4_id", "dec1_id", "dec2_id"    │
│      ]                                    │
│    }                                      │
└───────────────────────────────────────────┘
```

---

## Decision Trace Lifecycle

```
┌─────────────────────────────────────────┐
│       1. BIRTH (Created)                 │
│  POST /decisions                         │
│  {                                       │
│    message_id: "msg_123",                │
│    agent_suggestion: {...},              │
│    human_action: {...},                  │
│    context_used: {...},                  │
│    why: "..."                            │
│  }                                       │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│       2. STORAGE (Persisted)             │
│  PostgreSQL:                             │
│  • decisions table (core data)           │
│  • decision_precedents (links)           │
│  • graph_nodes (visualization)           │
│  • graph_edges (connections)             │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│       3. RETRIEVAL (Queried)             │
│  Future messages trigger:                │
│  • Hybrid retrieval finds this decision  │
│  • Used as precedent for suggestions     │
│  • Informs agent reasoning               │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│       4. VISUALIZATION (Displayed)       │
│  • Appears in Decision History           │
│  • Shown in Context Graph                │
│  • Clickable for details                 │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│       5. INFLUENCE (Teaching)            │
│  This decision now teaches the agent:    │
│  • "Users with this pattern prefer X"    │
│  • Influences future suggestions         │
│  • Becomes part of system knowledge      │
└─────────────────────────────────────────┘

♻️  The more decisions, the smarter the agent!
```

---

## Reset Demo Flow

```
┌─────────────┐
│    USER     │
└──────┬──────┘
       │ 1. Click "Reset Demo" button
       │ 2. Confirm dialog
       ▼
┌─────────────────────────────────────────┐
│         Frontend                         │
│  POST /reset                             │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│         FastAPI Backend                  │
│  DELETE FROM decision_precedents;        │
│  DELETE FROM decisions;                  │
│  DELETE FROM graph_nodes;                │
│  DELETE FROM graph_edges;                │
│  (Keep messages table intact)            │
└──────┬──────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────┐
│         Result                           │
│  • All decisions erased                  │
│  • Graph cleared                         │
│  • Messages remain                       │
│  • System back to "no precedent" state   │
│  • Ready for fresh demo                  │
└─────────────────────────────────────────┘

🔄 Perfect for demonstrating before/after learning!
```

---

## Key Takeaways

1. **First Decision**: No precedent → base logic → learning starts
2. **Nth Decision**: Precedent exists → hybrid retrieval → pattern applied
3. **Graph**: Every decision becomes a node → connected by precedent edges
4. **Reset**: Clear decisions → demonstrate learning from scratch

The magic is in the **feedback loop**: human decisions → stored precedent → improved suggestions → more decisions → better precedent → even better suggestions!

